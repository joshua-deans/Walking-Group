<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.cmpt276.walkinggroupindigo.walkinggroup">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".app.ServerTestActivity" />
        <activity android:name=".app.ApiKeyActivity" />
        <activity android:name=".app.MapsActivity" />
        <activity android:name=".app.LoginActivity" />
        <activity android:name=".app.SignUpActivity" />
        <activity android:name=".app.ManageMonitoring" />
        <activity android:name=".app.DashboardActivity" />
        <activity android:name=".app.AddMonitoringActivity"></activity>
    </application>

</manifest>